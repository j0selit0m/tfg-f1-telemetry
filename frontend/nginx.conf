# Configuración de Nginx para SPA (Single Page Application).
# Redirige todas las rutas a index.html para que React
# gestione el routing en el navegador.
server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}